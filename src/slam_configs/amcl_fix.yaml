amcl:
  ros__parameters:
    use_sim_time: false

    # 🔴 이게 핵심
    base_frame_id: "base_footprint"
    odom_frame_id: "odom"
    global_frame_id: "map"

    scan_topic: scan

    min_particles: 200
    max_particles: 2000
    update_min_d: 0.2
    update_min_a: 0.2
    resample_interval: 1
